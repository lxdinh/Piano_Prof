# Flutter / Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
*.iml

# IDE
.idea/
.vscode/

# Platforms we don't currently target (regenerate with `flutter create .`)
ios/
linux/
macos/
windows/

# Firebase config (do not commit secrets)
lib/firebase_options.dart
android/app/google-services.json
ios/Runner/GoogleService-Info.plist

# NOTE: android/ and web/ ARE committed (they hold required config:
# permissions, compileSdk 36, build-dir workaround). pubspec.lock is committed
# for reproducible cloud builds. android/.gitignore still excludes its build
# outputs + local.properties.
